<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Server - <%= server.name %></title>
    <link rel="icon" href="https://i.postimg.cc/y87h3x7y/0dc15569002a714e09ef3b03da9ac43d-removebg-preview.png" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: "Poppins", sans-serif;
            background-color: #121212;
            color: #e0e0e0;
            margin: 0;
            padding: 0;
            background-image: url("https://i.postimg.cc/y87h3x7y/0dc15569002a714e09ef3b03da9ac43d-removebg-preview.png");
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: -1;
        }

        h1 {
            color: #00bcd4;
            font-size: 3rem;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

        .server-info {
            text-align: center;
            padding: 20px;
        }

        .server-info img {
            border-radius: 10px;
        }

        .command-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 40px;
            width: 100%;
            max-width: 1000px;
            text-align: center;
        }

        .command-item {
            padding: 20px;
            background-color: #333;
            border-radius: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .command-item:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 20px rgba(0, 188, 212, 0.5);
        }

        .btn {
            display: inline-block;
            margin-top: 15px;
            padding: 10px 20px;
            background: #00bcd4;
            color: white;
            text-decoration: none;
            font-weight: 600;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .btn:hover {
            background: #0097a7;
        }

        footer {
            margin-top: 40px;
            font-size: 1rem;
            color: #e0e0e0;
        }
    </style>
</head>
<body>
    <div class="server-info">
        <h1>Manage <%= server.name %></h1>
        <img src="https://cdn.discordapp.com/icons/<%= server.id %>/<%= server.icon %>.png" alt="<%= server.name %> Icon" width="100" height="100">
        <p>Server ID: <%= server.id %></p>
    </div>

    <div class="command-list">
        <div class="command-item">
            <h3>Kick User</h3>
            <p>Kicks a user from your server. Use this to remove disruptive members.</p>
            <a href="/dashboard/<%= server.id %>/kick" class="btn">Kick User</a>
        </div>

        <div class="command-item">
            <h3>Ban User</h3>
            <p>Bans a user from your server permanently. Use with caution!</p>
            <a href="/dashboard/<%= server.id %>/ban" class="btn">Ban User</a>
        </div>

        <div class="command-item">
            <h3>Send Announcement</h3>
            <p>Sends an important announcement to all members of the server.</p>
            <a href="/dashboard/<%= server.id %>/announce" class="btn">Send Announcement</a>
        </div>

        <div class="command-item">
            <h3>Clear Messages</h3>
            <p>Clears a certain number of messages from a channel to keep the server clean.</p>
            <a href="/dashboard/<%= server.id %>/clear" class="btn">Clear Messages</a>
        </div>

        <div class="command-item">
            <h3>Change Prefix</h3>
            <p>Change the command prefix for your bot in this server.</p>
            <a href="/dashboard/<%= server.id %>/prefix" class="btn">Change Prefix</a>
        </div>

        <!-- Add more command items as needed -->

    </div>

    <footer>&copy; 2024 Your Bot Dashboard. All rights reserved.</footer>
</body>
</html>
