<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="icon" href="https://i.postimg.cc/y87h3x7y/0dc15569002a714e09ef3b03da9ac43d-removebg-preview.png" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: "Poppins", sans-serif;
            background-color: #121212;
            color: #e0e0e0;
            margin: 0;
            padding: 0;
            background-image: url("https://i.postimg.cc/y87h3x7y/0dc15569002a714e09ef3b03da9ac43d-removebg-preview.png");
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: -1;
        }

        nav {
            position: absolute;
            top: 20px;
            left: 20px;
            right: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1.1rem;
        }

        .nav-left a, .nav-right a {
            color: #00bcd4;
            text-decoration: none;
            margin-left: 15px;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        .nav-left a:hover, .nav-right a:hover {
            color: #0097a7;
        }

        .nav-right {
            margin-left: auto;
        }

        h1 {
            color: #00bcd4;
            font-size: 3rem;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

        .content {
            text-align: center;
            padding: 20px;
            max-width: 800px;
        }

        .server-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .server-list li {
            margin: 10px 0;
        }

        .server-list a {
            color: #00bcd4;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.2rem;
            transition: color 0.3s ease;
        }

        .server-list a:hover {
            color: #0097a7;
        }

        footer {
            margin-top: 40px;
            font-size: 1rem;
            color: #e0e0e0;
        }
    </style>
</head>
<body>
    <nav>
        <div class="nav-left">
            <a href="/">Home</a>
            <a href="/about">About</a>
            <a href="/commands">Commands</a>
            <a href="/help">Help</a>
            <a href="/status">Status</a>
        </div>
        <div class="nav-right">
            <a href="/dashboard">Dashboard</a>
            <a href="/add-bot">Add Bot</a>
        </div>
    </nav>

    <div class="content">
        <h1>Dashboard</h1>
        <p>Welcome, <%= user.username %>! Here are the servers you can manage:</p>
        <ul class="server-list">
            <% servers.forEach(server => { %>
                <li>
                    <a href="/dashboard/<%= server.id %>">
                        <%= server.name %>
                    </a>
                </li>
            <% }); %>
        </ul>
    </div>

    <footer>&copy; 2024 Your Bot Dashboard. All rights reserved.</footer>
</body>
</html>
