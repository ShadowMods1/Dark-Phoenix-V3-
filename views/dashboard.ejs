<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard | ShadowMods Pro</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        background-color: #121212;
        color: #e0e0e0;
        margin: 0;
        padding: 0;
        text-align: center;
      }

      nav {
        padding: 10px;
        background-color: #1e1e1e;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      nav a {
        color: #00bcd4;
        text-decoration: none;
        font-weight: 600;
        margin: 0 15px;
        transition: color 0.3s;
      }

      nav a:hover {
        color: #0097a7;
      }

      .content {
        padding: 20px;
      }

      .server {
        background: #1e1e1e;
        padding: 15px;
        margin: 10px auto;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        max-width: 600px;
      }

      .server img {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        margin-right: 15px;
      }

      .server-info {
        display: flex;
        align-items: center;
        flex-grow: 1;
      }

      .server-name {
        font-size: 1.2rem;
        font-weight: bold;
      }

      .manage-btn {
        text-decoration: none;
        color: #fff;
        background: #00bcd4;
        padding: 8px 15px;
        border-radius: 5px;
        font-weight: bold;
        transition: background 0.3s;
      }

      .manage-btn:hover {
        background: #0097a7;
      }
    </style>
  </head>
  <body>
    <nav>
      <div>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/commands">Commands</a>
        <a href="/help">Help</a>
      </div>
      <div>
        <a href="/auth/logout">Logout</a>
      </div>
    </nav>

    <div class="content">
      <h1>Welcome, <%= user.username %>!</h1>
      <p>Select a server to manage:</p>

      <% if (servers.length > 0) { %>
        <% servers.forEach((server) => { %>
          <div class="server">
            <div class="server-info">
              <img src="https://cdn.discordapp.com/icons/<%= server.id %>/<%= server.icon %>.png" alt="<%= server.name %>" />
              <span class="server-name"><%= server.name %></span>
            </div>
            <a class="manage-btn" href="/dashboard/<%= server.id %>">Manage</a>
          </div>
        <% }) %>
      <% } else { %>
        <p>No servers available for management.</p>
      <% } %>
    </div>
  </body>
</html>
